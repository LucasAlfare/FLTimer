fun main() {
}

/*
me ajude a montar minhas instruções SQL para meu banco SQLite para controlar os dados:

Solve: contém as propriedades propriedades "time" (numérico Long), "scramble" (string), comment (string), penalty (um de três: "ok", "plus_two", "dnf");

Session: basicamente uma lista de objetos Solve mas também tem uma propriedades de "name";

Sessions: basicamente uma lista de sessões.

Lembre-se que vou precisar ter meios de obter Solve de uma determinada Session.
 */